[project]
name = "the-baitome-sim"
version = "0.1.0"
description = "The Baitome registry as a formal object: element-action affinities, compound signatures, family recovery, identifiability limits, empty cells, and the creator-platform ecology"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
